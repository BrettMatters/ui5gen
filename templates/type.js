sap.ui.define([
    "{{parentType}}"
  ], function (ParentType) {
    'use strict'
    return ParentType.extend('{{typeNamespace}}', {
        formatValue(value, internalType){            
        },
        
        parseValue(value, internalType){            
        },

        validateValue(value){            
        }
    })
  })
  